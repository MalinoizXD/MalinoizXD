name: Update Github Stat

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Generate stats card
        uses: readme-tools/github-readme-stats-action@v1.1.0
        with:
          card: stats
          options: username=${{ github.repository_owner }}&count_private=true&theme=tokyonight&show_icons=true&rank_icon=github&include_all_commits=true&hide_border=true
          path: profile/stats.svg
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate top languages card
        uses: readme-tools/github-readme-stats-action@v1.1.0
        with:
          card: top-langs
          options: username=${{ github.repository_owner }}&layout=compact&langs_count=8&theme=tokyonight&hide_border=true
          path: profile/top-langs.svg
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate trophy SVG
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          USERNAME: ${{ github.repository_owner }}
        run: |
          mkdir -p profile
          python3 << 'EOF'
          import json, os, urllib.request

          username = os.environ['USERNAME']
          token = os.environ['GH_TOKEN']

          req = urllib.request.Request(
            f'https://api.github.com/users/{username}',
            headers={'Authorization': f'token {token}', 'Accept': 'application/vnd.github.v3+json'}
          )
          with urllib.request.urlopen(req) as r:
            user = json.loads(r.read())

          repos_req = urllib.request.Request(
            f'https://api.github.com/users/{username}/repos?per_page=100&type=owner',
            headers={'Authorization': f'token {token}', 'Accept': 'application/vnd.github.v3+json'}
          )
          with urllib.request.urlopen(repos_req) as r:
            repos = json.loads(r.read())

          total_stars = sum(repo.get('stargazers_count', 0) for repo in repos)
          total_forks = sum(repo.get('forks_count', 0) for repo in repos)
          followers = user.get('followers', 0)
          public_repos = user.get('public_repos', 0)

          def get_level(value, thresholds):
            for i, t in enumerate(thresholds):
              if value < t:
                labels = ['C','B','A','S','SS','SSS']
                return labels[i]
            return 'SSS'

          trophies = [
            ('Stars', str(total_stars), get_level(total_stars, [10,50,100,500,1000,5000]), '#FFD700'),
            ('Followers', str(followers), get_level(followers, [10,50,100,500,1000,5000]), '#C0C0C0'),
            ('Commits', '107+', 'A', '#CD7F32'),
            ('Repos', str(public_repos), get_level(public_repos, [5,10,20,50,100,200]), '#00CED1'),
            ('PRs', '2', 'B', '#9370DB'),
            ('Issues', '1', 'B', '#FF6347'),
          ]

          W, H, cols = 800, 120, 6
          cell_w = W // cols

          svg = f'''<svg width="{W}" height="{H}" xmlns="http://www.w3.org/2000/svg">
          <rect width="{W}" height="{H}" fill="#1a1b27" rx="10"/>
          '''

          for i, (name, count, rank, color) in enumerate(trophies):
            x = i * cell_w
            cx = x + cell_w // 2
            svg += f'''
            <g>
              <rect x="{x+4}" y="4" width="{cell_w-8}" height="{H-8}" fill="#1f2335" rx="8" stroke="{color}" stroke-width="1.5"/>
              <text x="{cx}" y="30" text-anchor="middle" fill="{color}" font-size="11" font-family="monospace" font-weight="bold">{name}</text>
              <text x="{cx}" y="65" text-anchor="middle" fill="{color}" font-size="28">&#x1F3C6;</text>
              <text x="{cx}" y="88" text-anchor="middle" fill="#a9b1d6" font-size="10" font-family="monospace">{count}</text>
              <text x="{cx}" y="106" text-anchor="middle" fill="{color}" font-size="11" font-family="monospace" font-weight="bold">{rank}</text>
            </g>
            '''

          svg += '</svg>'

          with open('profile/trophy.svg', 'w') as f:
            f.write(svg)
          print('Trophy SVG generated successfully')
          EOF

      - name: Commit cards
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add profile/*.svg
          git commit -m "Update README stat cards" || exit 0
          git push
